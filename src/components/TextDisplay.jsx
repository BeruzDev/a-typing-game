import React from 'react'
import AutoGeneratedText from '../components/typing/AutoGeneratedText'
import UserGeneratedText from '../components/typing/UserGeneratedText'

const TextDisplay = ({ isAutoGenerated, activeChar }) => {
    return (
        <div className='text-display'>
            {isAutoGenerated 
                ? <AutoGeneratedText activeChar={activeChar} /> 
                : <UserGeneratedText activeChar={activeChar} />}
        </div>
    )
}

export default TextDisplay
